{
  "name": "combined-project",
  "version": "1.0.0",
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "start": "concurrently \"cd backend && npm run init-db && npm start\" \"cd frontend && npm start\""
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "sqlite3": "^5.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
